dependencies{
    // 存储
    compile project(':jkmvc-orm')

    // jksoa-mq
    compile project(':jksoa-mq:jksoa-mq-client')
    // rabbitmq
    compile "com.rabbitmq:amqp-client:3.6.6"

    // 定时任务
    compile project(':jksoa-job')

    testRuntime "mysql:mysql-connector-java:5.1.6"
}
