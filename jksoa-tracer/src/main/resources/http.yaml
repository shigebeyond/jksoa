# 是否调试
debug: true
# controller类所在的包路径
# controller classes's package paths
controllerPackages:
    - net.jkcode.jksoa.tracer.web.controller
requestInterceptors: # 请求拦截器, 用于采集与发送span
    - net.jkcode.jksoa.tracer.agent.interceptor.HttpServerRequestInterceptor