dependencies{
    // other project
    if(env == 'dev'){
        testCompile project(':jkjob')
    }else{
        testCompile "net.jkcode.jkmvc:jkjob:$jkjob_version"
    }

    compile project(':jksoa-rpc:jksoa-rpc-registry')
}