dependencies{
    // other project
    compile project(':jkmvc-common')

    compile "org.jetbrains.kotlin:kotlin-stdlib-jre8:$kotlin_version"

    // zookeeper
    compile "org.apache.curator:curator-recipes:$curator_version"

    // netty
    compile "org.jboss.netty:netty:$netty_version"
}
