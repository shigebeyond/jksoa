
给一个统一的方法来做事件
interface event{
	fun pushEvent(name: String, data: Any?)
}

----------

在soa中抽取一个调度的接口
distributeToOne()
distributeToAll()

distributeShardings()

---------------


addCallback() 改为 setCallback(), 语义更严谨

----------------------

抽取 Invocation 接口, 统一 RpcRequest 与 Job 共同接口

job分lpc + rpc